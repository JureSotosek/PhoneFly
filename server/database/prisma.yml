endpoint: ${env:PHONEFLY_PRISMA_ENDPOINT}

datamodel: datamodel.graphql

hooks:
  post-deploy:
    - graphql get-schema --project database
    - graphql codegen

secret: ${env:PHONEFLY_PRISMA_SECRET}
