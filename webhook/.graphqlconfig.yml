projects:
  server:
    schemaPath: src/generated/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
      codegen:
        - generator: prisma-binding
          language: typescript
          output:
            binding: src/generated/client.ts
