overwrite: true
silent: true
schema: https://phonefly-api.now.sh/
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript-common'
      - 'typescript-client'
      - 'typescript-server'
config:
  scalars:
    DateTime: string
    Date: string
    Time: string
