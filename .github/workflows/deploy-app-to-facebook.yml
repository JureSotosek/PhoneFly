name: Deploy App To Facebook

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build App
        run: |
          cd packages/app
          yarn build
